/**
 * Layout for the authenticated app routes.
 * This layout wraps all routes under the (app) group.
 */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
